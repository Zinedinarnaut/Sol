diff --git a/src/Ryujinx/Headless/Options.cs b/src/Ryujinx/Headless/Options.cs
index a2dd6e4..b8deff3 100644
--- a/src/Ryujinx/Headless/Options.cs
+++ b/src/Ryujinx/Headless/Options.cs
@@ -4,6 +4,7 @@ using Ryujinx.Ava.Systems.Configuration;
 using Ryujinx.Ava.Systems.Configuration.System;
 using Ryujinx.Common.Configuration;
 using Ryujinx.Common.Configuration.Hid;
+using Ryujinx.Common.Configuration.Multiplayer;
 using Ryujinx.HLE;
 using Ryujinx.HLE.HOS.SystemState;
 using System;
@@ -82,6 +83,18 @@ namespace Ryujinx.Headless
             if (NeedsOverride(nameof(MultiplayerLanInterfaceId)))
                 MultiplayerLanInterfaceId = configurationState.Multiplayer.LanInterfaceId;
 
+            if (NeedsOverride(nameof(MultiplayerMode)))
+                MultiplayerMode = configurationState.Multiplayer.Mode;
+
+            if (NeedsOverride(nameof(MultiplayerDisableP2p)))
+                MultiplayerDisableP2p = configurationState.Multiplayer.DisableP2p;
+
+            if (NeedsOverride(nameof(MultiplayerLdnPassphrase)))
+                MultiplayerLdnPassphrase = configurationState.Multiplayer.LdnPassphrase;
+
+            if (NeedsOverride(nameof(MultiplayerLdnServer)))
+                MultiplayerLdnServer = configurationState.Multiplayer.LdnServer;
+
             if (NeedsOverride(nameof(DisableFileLog)))
                 DisableFileLog = !configurationState.Logger.EnableFileLog;
 
@@ -345,6 +358,18 @@ namespace Ryujinx.Headless
         [Option("lan-interface-id", Required = false, Default = "0", HelpText = "GUID for the network interface used by LAN.")]
         public string MultiplayerLanInterfaceId { get; set; }
 
+        [Option("multiplayer-mode", Required = false, Default = MultiplayerMode.Disabled, HelpText = "Local-wireless multiplayer mode.")]
+        public MultiplayerMode MultiplayerMode { get; set; }
+
+        [Option("multiplayer-disable-p2p", Required = false, Default = false, HelpText = "Routes online room connections through the master server.")]
+        public bool MultiplayerDisableP2p { get; set; }
+
+        [Option("multiplayer-passphrase", Required = false, Default = "", HelpText = "Private online room passphrase.")]
+        public string MultiplayerLdnPassphrase { get; set; }
+
+        [Option("ldn-server", Required = false, Default = "", HelpText = "Custom LDN room server.")]
+        public string MultiplayerLdnServer { get; set; }
+
         // Logging
 
         [Option("disable-file-logging", Required = false, Default = false, HelpText = "Disables logging to a file on disk.")]
diff --git a/src/Ryujinx/Headless/HeadlessRyujinx.Init.cs b/src/Ryujinx/Headless/HeadlessRyujinx.Init.cs
index 267ba1a..9f9d5c3 100644
--- a/src/Ryujinx/Headless/HeadlessRyujinx.Init.cs
+++ b/src/Ryujinx/Headless/HeadlessRyujinx.Init.cs
@@ -240,14 +240,14 @@ namespace Ryujinx.Headless
                         options.IgnoreMissingServices,
                         options.AspectRatio,
                         options.AudioVolume,
                         options.UseHypervisor ?? true,
                         options.MultiplayerLanInterfaceId,
-                        Common.Configuration.Multiplayer.MultiplayerMode.Disabled,
-                        false,
-                        string.Empty,
-                        string.Empty,
+                        options.MultiplayerMode,
+                        options.MultiplayerDisableP2p,
+                        options.MultiplayerLdnPassphrase,
+                        options.MultiplayerLdnServer,
                         options.EnableGdbStub,
                         options.GdbStubPort,
                         options.DebuggerSuspendOnStart,
                         options.CustomVSyncInterval
                     )
